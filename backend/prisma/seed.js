/**
 * PeoplePay360 - Unified Enterprise Database Seeder
 * 
 * Delegates directly to the complete 260-user, 1,900+ record enterprise dataset
 * ensuring that whether you run `node prisma/seed.js`, `npm run prisma:seed`, 
 * or `npx prisma db seed`, you always get the exact same verified dataset.
 */
require('./seed-enterprise');
